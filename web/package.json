{
  "name": "schoolpaper-workbench-web",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "node -e \"const fs=require('node:fs');fs.rmSync('dist',{recursive:true,force:true});fs.mkdirSync('dist');for(const file of ['index.html','styles.css','app.js'])fs.copyFileSync(file,'dist/'+file)\""
  }
}
